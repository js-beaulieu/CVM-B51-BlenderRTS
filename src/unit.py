class Unit():
	def __init__(self, idNb): #add color and position param

		self.idNb = idNb 
		#self.pos = pos
		#self.owner
		#self.dps etc...
		self.x = 0
        self.y = 0
        self.z = 0
        self.color = "color"
        self.state = 0 # 0 = stand still
        self.speed = 1
        self.destination = "" # get mouse right clic release

        def move(self):
        	#if state == 1 && destination:
        	pass


